Release 233 — GitHub browser upload instructions

Repository:
https://github.com/ssakthivel02/OmSaravanaBhava

MANDATORY BASE CHECK
Expected current main HEAD:
514eaf96ecaa53560bfd47cc8fc84ebd49e686f1

Do not upload if main has moved.

PACKAGE SIZE
- 43 actual Git changed files.
- 15 byte-identical support files included so local tests can run from the extracted ZIP.
- 58 total ZIP files.
- The support files should not appear in the Git commit because their bytes match Release 232.

DOWNLOAD AND EXTRACT
1. Download OmSaravanaBhava-Release-233.zip.
2. Verify the supplied ZIP SHA256.
3. Extract the ZIP completely.
4. Enable View > Show > Hidden items in Windows File Explorer.
5. Confirm the hidden .github directory is visible.
6. Upload the CONTENTS of the extracted folder, not the outer folder.

COMMIT METADATA — TWO SUPPORTED OPTIONS

OPTION A — PREFERRED EXACT SUBJECT
Enter this exact value in the small upper commit-title field:
Release 233: add browser-compatible commit attestation

Leave the description blank.
Expected commit-metadata result: PASS
Expected metadata_mode: exact-subject

OPTION B — GITHUB BROWSER FALLBACK
If GitHub keeps the subject as “Add files via upload”, enter this exact value as the FIRST NON-EMPTY LINE in the larger description field:
Release 233: add browser-compatible commit attestation

Do not put any greeting, explanation, space-only line or other text before it.
Expected commit-metadata result: WARN
Expected metadata_mode: browser-description-fallback
Expected overall governance status: PASS when all other checks pass

The fallback does not claim the subject is correct. The attestation will explicitly record subject_matches=false and body_matches=true.

POST-UPLOAD CHECKS
1. Copy the new main HEAD SHA.
2. Confirm its first parent is 514eaf96ecaa53560bfd47cc8fc84ebd49e686f1.
3. Open Actions and select “Release Governance Gate”.
4. Confirm overall status is PASS.
5. Inspect commit-metadata:
   - PASS for exact subject, or
   - WARN for the controlled browser fallback.
6. Confirm commit-parent is PASS.
7. Confirm changed-file-coverage is PASS.
8. Confirm undeclared and missing arrays are empty.
9. Confirm checksum-ledger and release-patch checks are PASS.
10. Download release-233-governance-evidence.
11. Open its attestation JSON and confirm one of these:
    a. subject_matches=true, body_matches may be false, metadata_mode=exact-subject; or
    b. subject_matches=false, body_matches=true, metadata_mode=browser-description-fallback.
12. Confirm “Release Independent Attestation” passes.
13. Retain both artifacts before Release 234.

AFTER RELEASE 233
Use the new local publisher for future releases whenever possible:

PowerShell:
./scripts/release/publish-release.ps1 -Manifest manifest-release-N.json

Bash:
scripts/release/publish-release.sh . manifest-release-N.json

These publishers create the exact Git subject and run strict metadata validation.

ROLLBACK
Revert the entire Release 233 commit. No production page or personal browser data should be changed by this governance-only release.
