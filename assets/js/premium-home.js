(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const animate=(el,target)=>{if(reduced){el.textContent=target;return}let v=0;const step=()=>{v=Math.min(target,v+Math.max(1,Math.ceil(target/36)));el.textContent=v;if(v<target)requestAnimationFrame(step)};step()};const load=async el=>{try{const r=await fetch(el.dataset.countSource,{credentials:'same-origin',headers:{Accept:'application/json'}});if(!r.ok)throw new Error(String(r.status));const p=await r.json();animate(el,Array.isArray(p)?p.length:0)}catch{el.textContent='—'}};addEventListener('DOMContentLoaded',()=>document.querySelectorAll('[data-count-source]').forEach(load));})();
