const temple=document.getElementById('temple'),q=document.getElementById('question'),answer=document.getElementById('answer');let items=[];fetch('data/guide/local-guide-index.json').then(r=>r.json()).then(d=>{items=d.records;temple.innerHTML=items.map(x=>`<option value="${x.id}">${x.name}</option>`).join('')});document.getElementById('ask').onclick=()=>{const x=items.find(r=>r.id===temple.value);answer.innerHTML=`<h2>${x.name}</h2><p>${x.summary}</p><p><strong>Available:</strong> ${x.available.join(', ')}</p><p><strong>Not confirmed:</strong> ${x.unavailable.join(', ')}</p><p>Your question: ${q.value||'No question entered'}</p>`}