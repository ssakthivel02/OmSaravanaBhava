let items=[];const status=document.getElementById('status'),out=document.getElementById('festivals');function render(){const v=status.value;out.innerHTML=items.filter(x=>!v||x.status===v).map(x=>`<article class="festival"><h2>${x.name_ta} · ${x.name}</h2><p>${x.description}</p><span>${x.status.replaceAll('_',' ')}</span></article>`).join('')}fetch('data/festivals/festival-knowledge.json').then(r=>r.json()).then(d=>{items=d.records;render()});status.onchange=render;