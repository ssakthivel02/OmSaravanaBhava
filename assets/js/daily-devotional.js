(()=>{'use strict';document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('dailyDevotional');if(!root)return;const data=await fetch('data/daily-devotional.json').then(r=>r.json());const day=Math.floor(Date.now()/86400000);const item=data[day%data.length];root.innerHTML=`<span class="pill">Daily rotation</span><h2 lang="ta">${item.titleTa}</h2><h3>${item.titleEn}</h3><div class="verse" lang="ta">${item.textTa}</div><p>The rotation is deterministic and is not a Panchangam calculation.</p>`})})();