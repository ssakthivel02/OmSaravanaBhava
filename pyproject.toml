
[project]
name = "omsaravanabhava-production-controls"
version = "244.0.0"
requires-python = ">=3.12"
description = "Permanent production baseline, observability, deployment attestation and repository integrity controls."

[tool.ruff]
line-length = 100
target-version = "py312"
