# Architecture

The baseline contract defines approved workflows, permanent controls, forbidden
legacy paths and the required service-worker identity. The validator supports
package, staged and final Git states without browser-upload fallbacks.
