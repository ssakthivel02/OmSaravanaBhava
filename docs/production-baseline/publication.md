# Publication

Production releases are published from a clean clone in one Git transaction.
Archive upload through the GitHub browser is not an accepted release mechanism
because it cannot express tracked-file deletion.
