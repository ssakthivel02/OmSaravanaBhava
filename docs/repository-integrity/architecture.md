# Architecture

The integrity pipeline discovers tracked paths, applies path and extension rules, checks required governance assets, validates the latest release manifest and emits JSON, inventory and SARIF evidence.
