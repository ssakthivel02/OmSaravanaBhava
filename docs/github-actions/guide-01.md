# GitHub Actions Static Validation Guide 1

Use CI to reject broken HTML, invalid JSON, duplicate paths, incorrect manifests and checksum failures before merging.

The current GitHub repository and live domain are the source of truth for operational evidence.
