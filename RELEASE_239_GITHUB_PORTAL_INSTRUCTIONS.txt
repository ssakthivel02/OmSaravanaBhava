Release 239 — mandatory one-command atomic publisher

APPROVED BASE
530ad97c68b6e7b8cbe997f2b6bbaf440ec5d527

EXACT COMMIT TITLE
Release 239: atomically reconcile repository state and establish verifiable publishing

DO NOT UPLOAD THIS ZIP THROUGH THE GITHUB BROWSER.
Browser upload cannot execute the fourteen required deletions.

WINDOWS PROCEDURE
1. Extract OmSaravanaBhava-Release-239.zip outside every Git repository.
2. Double-click PUBLISH_RELEASE_239.cmd.
3. Complete GitHub sign-in when Git Credential Manager prompts.
4. Keep the command window open until the final commit and push succeed.

The publisher automatically:
- creates a unique fresh clone;
- verifies HEAD is exactly 530ad97c68b6e7b8cbe997f2b6bbaf440ec5d527;
- copies only manifest-declared additions and modifications;
- verifies every deletion target is currently tracked;
- removes all fourteen targets using git rm;
- verifies exactly 71 staged paths and every Git status;
- runs 18 JavaScript and 152 Python tests;
- runs atomic-publisher, deployment, hygiene and integrity gates;
- creates the exact Release 239 commit;
- runs strict post-commit governance;
- pushes once to origin/main.

EXPECTED FINAL COMMIT
- 71 changed paths
- 40 additions
- 17 modifications
- 14 deletions
- no tracked .pyc or __pycache__
- no assets/js/site-directory.js
- service-worker cache release 239

OPTIONAL CUSTOM CLONE DIRECTORY
PUBLISH_RELEASE_239.cmd "C:\Project\OmSaravanaBhava-release239"

BASH OR GIT BASH
scripts/release239/publish-release-239.sh /path/to/clean/release239-clone

The publisher stops without changing main when the remote base moved, a deletion
target is missing, staged coverage differs, tests fail, or authentication fails.
